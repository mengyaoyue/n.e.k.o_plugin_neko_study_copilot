# 学习辅助猫娘（neko_study_copilot）

一个会规划、会出题、会看卷子、还会做心理疏导的 N.E.K.O 学习插件。

面向 **14 类考试**：高考 / 中考 / 四六级 / 考研 / 校内期末，以及大学生专属的
**大学期末（学分制 · GPA）**、**教师资格证**、**软考**、**注册会计师**、**法考**、
**雅思 / 托福**、**全国计算机等级考试**。

**用你自己的真实数据**做规划，而不是给你一套通用模板。

---

## 它能做什么

### 1. 规划：按「还剩多少分可拿」分配时间

不是平均分配，而是算清楚每门课离满分还差多少：

```
压力值 = (1 - 加权掌握度) × 该科满分
```

数学 150 分、物理 100 分，同样一半不会，数学就该拿更多时间。

- 按剩余天数自动划分**基础 → 强化 → 冲刺 → 临考**四个阶段；
- 周计划按科目轮转，不会出现"连着三周全是语文"；
- 每天开场固定 20% 时间给**到期复习**（艾宾浩斯间隔 1/2/4/7/15 天）；
- 每阶段末尾设检查点，测完更新掌握度，下一阶段自动重排。

#### 大学生：学分制是两套压力，得拆开算

大学和高中最大的区别是：**及格只要 60 分，但绩点决定保研、奖学金和出国**。
所以插件对大学考试用的是完全不同的口径：

```
学分系数 = 学分 ÷ 3        → 5 学分的课比 1 学分的课更值得投入
挂科风险 = 掌握度 < 62%    → 濒危科目时间 ×1.6，先抢救
时间压力 = (1 - 掌握度) × 满分 × 学分系数 × 风险系数
```

在面板（或配置 `credits`）里填上各科学分，例如 `math_adv:5,english:3,politics:3,cs:4`，
就会得到：

- 按学分加权的**时间分配**；
- 按学分加权的**绩点预估**（4.0 / 5.0 制）+ 保研 / 奖学金门槛定位；
- 一份**挂科风险清单**（按得分率升序，先保这些）。

覆盖的课程：高等数学、线性代数、概率论与数理统计、大学英语、思政课、
计算机与程序设计、大学物理、经管类（微观/宏观/管理学）、专业课。

#### 证书类考试

教资（综合素质 / 教育知识 / 学科教学）、软考（上午综合知识 + 下午案例或论文）、
注会（六科，60 分及格，五年内通过）、法考（客观题 180 + 主观题 108）、
雅思（四项均分 9 分制）、托福（合计 120）、NCRE（选择 40 + 上机 60）。
每类都自带合格线，预估时会按**实际计入科目的满分合计**等比缩放。

### 2. 联网：平台进度 + 免费公开资源

**平台连接**（面板里操作，只读）：

| id | 平台 | 说明 |
|---|---|---|
| `chaoxing` | 学习通（超星） | 常见需要图形验证码 |
| `zhihuishu` | 知到（智慧树） | 共享课与校内课域名不同 |
| `icourse163` | 中国大学 MOOC | 登录态在网易通行证域 |
| `xuetangx` | 学堂在线 | JSON 接口 |
| `jwc` | 教务系统（通用） | 需在面板填学校教务首页地址 |
| `neea` | 中国教育考试网 | 四六级 / 考研成绩，手动填身份证与姓名 |

**免费公开资源检索**（无需登录）：

国家中小学智慧教育平台、国家高等教育智慧教育平台、中国大学 MOOC、学堂在线、
B 站学习区、维基百科、Khan Academy、MIT OpenCourseWare。

### 3. 教学：三段式，且严格遵守命题规律

这是本插件最硬的一块。每个知识点都登记了**它只会以哪些题型出现**：

| 知识点 | 只会考成 | 不会考成 |
|---|---|---|
| 名篇名句默写 | 填空题 | 解答题、选择题 |
| 交变电流 | 选择题 | 计算题、大题 |
| 计数原理与二项式 | 选择 / 填空 | 解答题 |
| 导数及其应用 | 选择 / 填空 / 解答 | — （压轴载体） |
| 中值定理与导数应用（高数） | 选择 / 填空 / **证明** | 案例分析 |
| 图论（数据结构） | 选择 / 填空 / **编程** | 论述题 |
| 软考论文写作 | **论述** | 选择题 |
| 法考民法 | 选择 / **案例分析** | — |

共登记 **193 个知识点**、**14 种题型**（含大学特有的证明题、论述题、案例分析题、
编程/上机操作题）。出题前会先过一遍题型白名单，出错了直接拒绝并说明原因。
所以不会出现"这道题根本不会这么考"的废题。

三段难度：

- **基础**（掌握度 < 45%）：题干直接对应定义，一步到位，不设坑；
- **中期**（45% ~ 75%）：允许设坑，但坑必须来自该点登记的真实易错点
  （定义边界、使用条件、符号方向），**不靠文字游戏**；
- **后期**（> 75%）：综合创新；若该点不是压轴载体，会明确要求"在允许题型内创新，
  别硬凑成解答题"。

### 4. 诊断：三档分数预期，说清楚依据

```
现在就考：约 317 分
最坏预期：约 312 分（发挥失常 + 题目变形）
最  可  能：约 353 分
较好预期：约 381 分（按计划执行到位）
波动区间：±25 分
```

模型链路是**掌握度 → 得分率 → 分数**，而不是拍脑袋给数：

1. 按分值权重算各科加权掌握度；
2. 把剩余学习时间按权重分给各知识点，`增益 ∝ 还剩多少没掌握`（自带边际递减）；
3. 三档分别对应"计划执行 35% / 65% / 100%"；
4. 临场波动按总分 4.5% 计；通过线按**实际计入科目的满分合计**缩放
   （只选了语数英物时，750 制的本科线不能直接套）。

依据会逐条写出来，包括"这个结论是基于多少条作答记录"。

### 5. 疏导：具体到今天能做什么

不说"你一定可以的"。按考试类型区分压力源：

- 高考 → 长跑倦怠、意义感流失
- 中考 → 年纪小、靠环境设计而非意志力
- 四六级 → 拖延型焦虑、没有过程反馈
- 考研 → 孤立无援、长期没有外部反馈
- 大学期末 → 「及格就行」与「绩点要好看」是两套压力，且没人盯着你学
- 注会 → 长周期消耗，六科五年，很容易形成挫败性归因
- 法考 → 体量型压力 + 「客观题没过就白学一年」的门槛结构
- 雅思 / 托福 → 单项瓶颈（口语写作）/ 时间压迫（三小时机考）
- 计算机等级 → 轻视型压力，卡在上机操作没真练过

七种情绪状态各有对应的认知重构与三条可执行动作。**检测到自伤/长期失眠等风险信号时，
立即停止打鸡血，转为给出真实热线**（12355 青少年服务台、400-161-9995 希望 24 热线）。

---

### 6. 识图：聊天框发截图就行

这是猫娘，主要就该在聊天框里交流——**截图发过去，别手打复制**。

```
你：[截图]                       ← 题目 / 试卷 / 作业 / 成绩单
你：这套卷子我哪里弱？
猫娘：已读取你发的截图并转写如下
     第 1 题（3 分）… 第 2 题…
     ======== 诊断 ========
     导数及其应用 掌握度 38% → …
```

链路：`@message(source="chat")` 记住你最近发的文本与图片 → 视觉模型把图**转写成文字**
（保留题号、选项、分值、图注，认不出的地方标〔无法辨认〕）→ 再走常规诊断 / 讲解链路。

- 认三种图片来源：消息里的内存字节、本地路径、http 地址，都会转成 data URL；
- 优先用宿主 **vision 档**模型，该档没配就自动退回 **conversation** 档；
- 面板「诊断预估」页也留了手动上传入口（`/api/vision`），但**推荐走聊天框**；
- 关掉 `vision_enabled` 就完全不改聊天消息，只记文本。

### 7. 面板

`plugin.toml` 里声明了 `[plugin.ui]`，宿主会在插件管理里给出「打开界面」入口。
页面由宿主托管时同源相对路径会 404，所以前端会先探测宿主代理，再探测插件自己的
`127.0.0.1:<panel_port>`，**两边都通**才认为服务可用。

## 安装与配置

1. 在 N.E.K.O 的插件管理里安装，或手动把本目录放到
   `N.E.K.O/plugins/neko_study_copilot/` 与 `N.E.K.O/.neko-plugin-installations/plugins/neko_study_copilot/`
   （**两个位置都要放**，宿主实际从后者加载）；
2. 打开插件面板（插件管理 → 面板 → 打开界面），填好档案：考试类型、地区、科目、目标分、考试日期；
3. 需要同步平台时，在「平台连接」页填本人账号密码；需要验证码时看图输入。

面板端口默认 `15880`，被占用会自动往后找。

## 怎么用自然语言叫它

直接说话就行，猫娘会自己选能力：

- **直接在聊天框发题目 / 试卷 / 成绩单截图**，再说「识图诊断」「讲讲这道题」
  「这套卷子我哪里弱」→ 视觉模型转写题目 → 诊断 / 讲解，**不用手打复制**
- 「帮我规划一下接下来怎么复习」→ 生成学习计划
- 「导数这块给我出几道题」→ 按命题规律出题
- 「讲一下动量守恒，顺便来道例题」→ 讲解 + 配套例题
- 「这次数学 92/150，导数大题没写出来」→ 诊断薄弱点
- 「我大概能考多少分，能不能过」→ 三档分数预期
- 「我这学期会不会挂科，绩点能到多少」→ 挂科风险清单 + 学分加权绩点
- 「我要考教资 / 软考 / 注会 / 法考 / 雅思」→ 自动切到对应考试画像
- 「高数中值定理给我出道证明题」→ 按大学题型白名单出题
- 「最近学不动，分数一直不涨」→ 心理疏导
- 「同步一下学习通，看看还有多少课没看」→ 平台同步
- 「找点导数的免费课」→ 免费资源检索

## 数据与安全

- 所有数据落在本机 `data/study.db`，账号凭据在 `data/credentials.json`
  （用管理员密码做轻量混淆）；`data/` 与 `config/` 已被 `.gitignore` 排除；
- 平台连接**只读取本人账号下的数据**，只做 GET 与登录 POST，不写入任何业务数据；
- 遇到验证码一律回传图片地址由本人输入，**不逆向加密参数、不做验证码识别**；
- 同站请求强制最小间隔（默认 1.5 秒），尊重 `robots.txt`（可在配置关闭）；
- 大模型走 N.E.K.O 已配置的会话模型，插件不保存第二份密钥。

> 请仅用于访问你本人有权限的账号数据。课程平台的使用条款各不相同，
> 是否连接由你自己判断并承担相应责任。

## 目录结构

```
__init__.py        主插件类：配置、生命周期、LLM 工具与入口、面板接口
_crawl.py          抓取层：限速 / 重试 / Cookie / robots / 正文抽取
_connectors.py     平台连接管理：凭据存储 + 登录 + 读取
_platforms.py      平台适配器（新增平台只需加一条 PlatformSpec）
_sources.py        免费公开资源检索
_profiles.py       考试画像：科目、题型、分值、地区差异
_syllabus.py       考点图谱与命题规律（193 个知识点，含题型白名单与易错点）
_planner.py        学习计划生成
_diagnose.py       诊断与分数预估
_tutor.py          教学与出题引擎（三段式 + 题型硬约束）
_psych.py          心理暗示与疏导
_store.py          本地 SQLite 存储
_panel.py          面板 HTTP 服务与跨线程异步桥
static/index.html  面板页面
```

## 扩展

**新增考点**：往 `_syllabus.py` 的 `SUBJECT_ROWS[("科目", "学段")]` 里加一行即可：

```python
("知识点名", (FORM_CHOICE, FORM_FILL), "high", 3.0, 5.0,
 ("前置知识点",), ("易错点一", "易错点二"), False, "命题备注"),
#      名称      允许题型              频率  难度  权重  前置         易错点              压轴  备注
```

**新增平台**：往 `_platforms.py` 的 `PLATFORMS` 里加一条 `PlatformSpec`。

**新增考试类型**：在 `_profiles.py` 里写一个 `build_xxx() -> ExamProfile`，
注册进 `PROFILES` / `DEFAULT_SUBJECTS` / `EXAM_ALIASES`（中文别名），
必要时在 `_syllabus.py` 里补 `SUBJECT_ROWS[("科目", "大学")]` 的知识点行。
若新考试的卷子科目名与考点科目名不一致（如教资的 `quality`/`pedagogy`），
在 `SUBJECT_SYLLABUS_KEY` 里加一条归并映射即可。

## 开发

```bash
python tests/test_basic.py      # 纯逻辑测试，不需要 pytest
python -m pytest tests/ -q      # 冒烟测试
python -m ruff check .          # 官方市场检查用的 lint 参数
```

### ⚠️ 铁律：不要顶层 import「冷门」标准库

宿主是 **PyInstaller 冻结进程**，标准库里**只打包宿主自己（或其依赖）引用过的模块**。
插件和宿主共用同一个解释器，所以「标准库一定有」这个假设**不成立**。

线上真实事故：`_crawl.py` 顶层写了 `import urllib.robotparser`，宿主没打包这个模块，
插件在 import 阶段直接死掉。而宿主把子进程 stderr 吞了，日志里只剩
`PLUGIN_START_FAILED`，`logs/plugin/` 和插件 `data/` 都不生成，**一点线索都没有**。

约定：

```python
try:                       # 宿主有就用官方的
    from urllib.robotparser import RobotFileParser as _HostRobotParser
except Exception:          # 没有就用自带兜底，绝不连累插件启动
    _HostRobotParser = None
```

- 已确认宿主**缺少**：`urllib.robotparser`
- 已按此约定改造（不再依赖）：`http.server` → 自己用 `socket` 写面板服务；
  `gzip` / `zlib` → 解压失败就原样返回；`html` → 自带实体反转义；
  `concurrent.futures` → 缺了退化成串行。
- `tests/test_smoke.py::TestFrozenHostCompat` 会在子进程里**屏蔽这些模块**再 import 一次
  插件，并用 AST 检查禁掉顶层硬导入。新增 import 前先跑它。

### 排查启动失败的正确姿势

宿主吞 stderr，所以插件自己落盘：

- `%TEMP%\neko_study_copilot_crash_import.txt` —— import 阶段崩溃的完整 traceback
- `%TEMP%\neko_study_copilot_crash_startup.txt` —— `on_startup` 阶段
- `%TEMP%\neko_study_copilot_host_modules.txt` —— 宿主冻结环境里哪些标准库可用（探针）

## 许可

Apache-2.0，与 N.E.K.O 主项目一致。
